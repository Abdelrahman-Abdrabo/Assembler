import java.awt.*;
import java.io.*;
//read the text file
// find labels and call the SetLabel function.
// now loop again to assemble each line.
public class Main {
    public static void main(String[] args) {
//      Assembler AADM= new Assembler();
//      int Line= 0;
//      try{
//          BufferedReader in = new BufferedReader(new FileReader("Code.txt"));
//          String l =null;
//          int numberOfLabels=0;
//          while((l= in.readLine())!=null){
//              if(l.contains("#")) continue;
//              Line++;
//              if(l.contains(":")) {
//                  numberOfLabels++;
//                  AADM.SETLabel(l.replaceAll("[\\s :]", ""), Line-numberOfLabels);
//              }
//          }
//          System.out.println();
//          Line= 0;
//          l =null;
//          in = new BufferedReader(new FileReader("Code.txt"));
//          FileWriter writer= new FileWriter("OUTPUT.txt",false);
//          BufferedWriter bufferedWriter= new BufferedWriter(writer);
//          while((l= in.readLine())!=null){
//              if(l.contains(":")||l.contains("#")) continue;
//              String OUTPUT =AADM.assemble(l,Line);
//              bufferedWriter.write(OUTPUT);
//              bufferedWriter.newLine();
//              System.out.println(OUTPUT);
//              Line++;
//          }
//          bufferedWriter.close();
//      }catch (Exception e){
//          System.out.println(e.getMessage());
//      }
    }
}